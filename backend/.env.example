DATABASE_URL=postgresql://taskman:changeme@localhost:5432/taskman
JWT_ACCESS_SECRET=changeme_access_secret
JWT_REFRESH_SECRET=changeme_refresh_secret
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d
UPLOAD_DIR=./uploads
MAX_UPLOAD_SIZE_MB=25
PORT=4000
NODE_ENV=development

# --- Chỉ dùng khi chạy `npx prisma db seed` (tuỳ chọn, có giá trị mặc định) ---
# SEED_ADMIN_EMAIL=admin@company.local
# SEED_ADMIN_NAME=Quản trị viên
# SEED_ADMIN_PASSWORD=doi-mat-khau-nay-truoc-khi-len-production
# SEED_BOARD_NAME=Đội hạ tầng
# SEED_BOARD_PREFIX=INF
